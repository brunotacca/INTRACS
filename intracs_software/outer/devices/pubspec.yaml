name: intracs_devices
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue: ^0.8.0

  intracs_gateways:
    path: ../../adapter/gateways
  intracs_application:
    path: ../../inner/application
  intracs_entities:
    path: ../../inner/entities