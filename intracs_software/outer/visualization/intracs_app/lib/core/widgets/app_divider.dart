import 'package:flutter/material.dart';
import 'package:intracs_app/core/themes/app_colors.dart';

class AppDivider extends StatelessWidget {
  AppDivider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Divider(
      color: cyan,
    );
  }
}
